<h1>Game #<%= @game.id %></h1>

<%= render "score_table" %>

<p>
  <%= link_to "Keep Score", score_path(game_id: @game.id), { class: "btn btn-primary", style: "margin-right:40px" } %>
  <%= link_to "Create New Game", new_game_path, class: "btn btn-primary" %>
</p>
